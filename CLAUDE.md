当前已经有的技术栈
chat_model_name: qwen-plus
embedding_model_name: text-embedding-v4

# 代码结构与分析
3-每新写一个接口都要维护到接口文档里./api.md ，主要包括接口是什么，什么意思？入参数是哪个实体，返回参数是哪个实体。
2-代码必须写完整、正确、高可用，备注清晰明了，提高复用。
1-完整的CRUD 必须按照 controller，service ，serviceImpl,mapper.java,mapper.xml结构，swagger接口文档。

# 项目简介

我想做一个企业智能招聘客服，在x-productB服务。


核心架构

用户提问 → 意图识别 → 知识库检索(RAG) → LLM生成回答 → 返回用户

关键技术模块

1. 知识库构建（最重要）
- 把企业文档（FAQ、产品手册、规章制度）向量化存储
- 推荐：Elasticsearch 或 Milvus（向量数据库）
- 文档解析：PDF/Word/网页 → 切片 → Embedding

2. RAG（检索增强生成）
- 用户问题 → Embedding → 向量相似度检索 → 召回相关文档
- 把检索结果 + 用户问题一起喂给 LLM
- 这样 LLM 回答的是你企业自己的内容，不会瞎编

3. LLM 接入
- 国内：DeepSeek（便宜）、通义千问、文心一言
- 国外：Claude API、GPT-4
- 你们已经在用 AI 服务，这块有基础

4. 会话管理
- 你们已经有融云 IM + 行为订单这套，可以直接复用
- 多轮对话上下文管理

  技术选型建议（基于你们现有栈）

  Spring Boot（现有）
  ↓
  LangChain4j 或 Spring AI（Java生态的RAG框架）
  ↓
  DeepSeek API（性价比最高）
  ↓
  Milvus / PGVector（向量存储）

  落地步骤

1. MVP 先跑通：把 20 条 FAQ 做成知识库，接 DeepSeek，能问能答
2. 优化召回：关键词 + 向量混合检索，提升准确率
3. 兜底机制：答不上来转人工，记录未命中问题持续补充知识库
4. 评估体系：建立问答准确率的评测集，迭代优化

最大的坑

- 知识库质量 > 模型能力，烂数据给再好的模型也没用
- 切片策略很重要，切太短丢上下文，切太长噪声多
- 别一开始就追求完美，先跑通闭环


---
你们现在已经有 AI 机器人 + IM 的基础，做这个其实有很大优势。想从哪个模块开始？

# 如何优化召回：关键词 + 向量混合检索，提升准确率

⏺ 这是 RAG 里最核心的问题，展开说：

混合检索原理

用户问题
↓              ↓
关键词检索      向量检索
(BM25/ES)    (语义相似度)
↓              ↓
召回结果A    召回结果B
↓
RRF融合排序
↓
Top-K结果
喂给LLM

两种检索的互补性

┌──────────┬──────────────────────────────┬────────────────────────────┐
│          │          关键词检索          │          向量检索          │
├──────────┼──────────────────────────────┼────────────────────────────┤
│ 擅长     │ 精确词匹配、专有名词、编号   │ 语义理解、同义词、模糊表达 │
├──────────┼──────────────────────────────┼────────────────────────────┤
│ 失败场景 │ "怎么请假" vs "假期申请流程" │ "订单号SN20241201"         │
├──────────┼──────────────────────────────┼────────────────────────────┤
│ 工具     │ Elasticsearch BM25           │ Milvus/PGVector            │
└──────────┴──────────────────────────────┴────────────────────────────┘

具体优化手段

1. 查询改写（Query Rewriting）
   用户问："请假要找谁审批"
   ↓ LLM改写
   改写为：["请假审批流程", "假期申请审批人", "HR审批"]
   多个角度同时检索，提升召回覆盖

2. RRF 融合排序
   // Reciprocal Rank Fusion
   score = Σ 1/(k + rank_i)  // k通常取60

// 关键词结果：文档A排第1，文档B排第3
// 向量结果：  文档B排第1，文档A排第4
// 融合后：文档B综合得分更高

3. 重排序（Rerank）
- 召回 Top-20，用 Rerank 模型精排取 Top-5
- 推荐模型：bge-reranker（开源免费）
- 效果提升明显，这步很值得做

4. 切片策略优化
   ❌ 固定长度切片（丢上下文）

✅ 按语义切片：
- 一个完整问答为一个chunk
- 父子chunk：检索小chunk，返回父chunk给LLM
- 重叠切片：chunk间保留100字重叠区

5. 元数据过滤（先过滤再检索）
   // 先用业务标签缩小范围，再做向量检索
   filter: {department: "HR", doc_type: "policy"}
   → 在这个子集里做向量检索
   // 大幅减少噪声

## 效果评估体系（很重要）

建立测试集：100个真实问题 + 标准答案
↓
评估指标：
- Recall@K：Top-K里有没有正确文档
- MRR：正确文档排在第几位
- 端到端准确率：最终回答对不对
  ↓
  每次改动后跑一遍，看数字变化

落地优先级

第一步：跑通向量检索基础版
第二步：加 BM25 混合 + RRF 融合  ← 收益最大
第三步：加 Rerank 精排            ← 性价比高
第四步：查询改写                  ← 锦上添花
第五步：元数据过滤                ← 按业务需要

推荐技术组合

向量数据库: PGVector（已有PostgreSQL直接用）
或 Elasticsearch 8.x（自带向量+BM25）
Rerank模型: bge-reranker-v2-m3（本地部署，免费）
RAG框架:   LangChain4j（Java，和Spring Boot无缝集成）
LLM:       DeepSeek（召回后生成，便宜）

---
最实用的建议：先用 Elasticsearch 同时开 BM25 + 向量检索，加 RRF 融合，这一步做好准确率能从 60% 提到 85% 左右。Rerank 再往上推 5-10%。

想深入哪个环节？
